//*Variables globales//*
// Seleccionar elementos del DOM
let numeroMaximo = 10;

//array
let amigos = [];

//*Agregar nombre y verifcar que no este vacio
function validarNombre() {
    let name = document.getElementById(amigo).value;
    if ( name === "") {
        alert("Añade un nombre válido")
    } else {
        console.log("Nombre añadido:" + name)
    }
    return
}

